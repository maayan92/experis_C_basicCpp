#include <stdio.h>

/*
	Description: return the average number of the array values.
	Input: _arr - integer array, _size - size of the array.
	Return value: return the average number of the array value.
*/
double AverageArray(int *_arr, int _size);

/*
	Description: check which value occur the most in the array.
	Input: _arr - integer array, _size - size of the array.
	Return value: return the most occur value.
*/
int MostOccur(int *_arr, int _size);

/*
	Description: 
	Input: _arr - integer array, _size - size of the array.
	Return value: 
*/
int NumOfEven(int *_arr, int _size);

/*
	Description: 
	Input: _arr - integer array, _size - size of the array.
	Return value: 
*/
void SortOneZero(int *_arr, int _size);


